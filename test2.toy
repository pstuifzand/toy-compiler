func test(x int32, y int32) int32 {
    if (x) {
        x = 2 * x + 10;
        x = x + x;
        if (x + 2) {
            x = x + x;
            x = x + (x+1);
        }
        x = x + 10;
    }
    return x;
}
